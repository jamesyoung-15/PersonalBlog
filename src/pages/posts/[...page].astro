---
// Astro way of doing pagination, currently not using this file, instead wrote own pagination in SearchPost.astro

// import BaseLayout from "../../layouts/BaseLayout.astro";
// const pageTitle = "Blog Posts";
// import { type CollectionEntry, getCollection } from "astro:content";
// import { Debug } from "astro:components";
// import type { GetStaticPaths } from "astro";
// import DynamicSearch from "../../layouts/DynamicSearch.astro";
// import PageButtons from "../../components/PageButtons.astro";

// export const getStaticPaths = (async ({ paginate }) => {
//   const posts = await getCollection("posts", ({ data }) => !data.draft);
//   return paginate(posts, { pageSize: 4 });
// }) satisfies GetStaticPaths;

// const page = Astro.props.page;

// const tags =  Array.from(
//   new Set(page.data.flatMap((post) => post.data.tags))
// );

---
<!-- <Debug value={page} /> -->
<!-- <BaseLayout pageTitle={pageTitle}> -->
    <!-- <SearchPost data={page.data}></SearchPost> -->
    <!-- <DynamicSearch page={page}></DynamicSearch> -->
    <!-- <PageButtons page={page} /> -->
<!-- </BaseLayout> -->

